import styled from '@emotion/styled';
import colors from '../utils/colors';

const InputCaption = styled.label`
  color: ${colors.captionFont};
  font-size: 1.2rem;
  font-weight: 400;
  display: flex;
  justify-content: start;
`;

export default InputCaption;
